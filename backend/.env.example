# --- PostgreSQL Configuration (Used by Docker Compose on initial setup) ---
# NOTE: For production, use a strong, randomly generated password.
POSTGRES_USER=t8d_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=t8d_db

# --- Application Database Connection Details ---
# Your backend application will use these variables to construct the connection string.
DB_HOST=localhost
DB_PORT=5432
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
DB_NAME=${POSTGRES_DB}
SERVER_PORT=3000

# --- Full Connection String for Prisma ---
# This URL is what Prisma will use to connect to the database.
# It reads from the variables defined above.
# The host is 'localhost' because we exposed the port '5432' to our host machine.
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"

# --- Authentication ---
# A strong, random secret key for signing JSON Web Tokens (JWTs)
# You can generate one using: openssl rand -base64 32
JWT_SECRET="your_super_secret_and_long_random_string_here"
